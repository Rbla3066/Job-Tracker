const geocoder = require('./index')({
  provider: 'google',
  // apiKey: 'Z0qsdC6THM0AFtc6kauBsktpIY0wglXn'
});

const BPromise = require('bluebird');

// BPromise.resolve()
//   .then(() => {
//     var test = [];
//     for (var i = 0; i < 20000; i++) {
//       test.push(i.toString());
//     }

//     return test;
//   }).map(() => {
//     return geocoder.geocode('4433 rue chambord').then(() => console.log('tatat')).catch(console.log);
//   }, {concurrency: 10});

geocoder.geocode({address: '4433 rue chambord'})
  .then(console.log);
